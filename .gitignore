*.swp
.DS_Store
.DS_Store?
.Spotlight-V100
.Trashes
._*
.bundle
.jekyll-cache/
.jekyll-metadata
.sass-cache/
Icon?
Thumbs.db
_cache
_site/
ehthumbs.db
vendor

# Gerado no momento do build (local ou CI) para identificar a versão
# publicada no rodapé do site — nunca committed.
_data/build.yml

# To prevent accidental push of translations from
# Transifex.
.tx/translations/

# Avoid pushing .gz files created during rsync
*.gz
